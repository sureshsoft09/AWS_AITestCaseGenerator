# AWS Configuration
AWS_REGION=us-east-1

# Option 1 (Recommended): Use AWS Profile - comment out credentials below and set AWS_PROFILE env var
# Set in terminal: set AWS_PROFILE=default (Windows) or export AWS_PROFILE=default (Linux/Mac)

# Option 2: Use explicit credentials (not recommended for production)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# Option 3: Use temporary credentials from AWS Console (expires in 1-12 hours)
# AWS_ACCESS_KEY_ID=ASIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_SESSION_TOKEN=...

# S3 Configuration
S3_INGEST_BUCKET=medassure-ingest-bucket
S3_FRONTEND_BUCKET=medassure-frontend-bucket

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=MedAssureAI_Artifacts

# OpenSearch Configuration
OPENSEARCH_ENDPOINT=https://f46t68rvbgb098nedxjh.us-east-1.aoss.amazonaws.com
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=your_password

# Jira Configuration
JIRA_URL=https://your-domain.atlassian.net
JIRA_API_TOKEN=your_jira_api_token
JIRA_EMAIL=your_email@example.com

# Bedrock Configuration
BEDROCK_MODEL_ID=anthropic.claude-v2
BEDROCK_REGION=us-east-1

# Application Configuration
LOG_LEVEL=INFO
ENVIRONMENT=development

# Email Notification (SES)
SES_SENDER_EMAIL=noreply@medassure.com
SES_REGION=us-east-1

# MCP Server Endpoints
JIRA_MCP_SERVER_URL=http://localhost:8001
DYNAMODB_MCP_SERVER_URL=http://localhost:8002
