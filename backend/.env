# AWS Configuration
AWS_REGION=us-east-1

# Option 1 (Recommended): Use AWS Profile - credentials loaded from ~/.aws/credentials
# After saml2aws login, boto3 automatically uses credentials from profile

# S3 Configuration
S3_INGEST_BUCKET=medassure-ingest-bucket
S3_FRONTEND_BUCKET=medassure-frontend-bucket

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=MedAssureAI_Artifacts

# OpenSearch Configuration
# For OpenSearch Serverless (.aoss.), uses AWS IAM authentication automatically
# For regular OpenSearch domain, uses username/password below
OPENSEARCH_ENDPOINT=https://f46t68rvbgb098nedxjh.us-east-1.aoss.amazonaws.com

# Application Configuration
LOG_LEVEL=INFO
ENVIRONMENT=development
BACKEND_PORT=8002

# Email Notification (SES)
SES_SENDER_EMAIL=noreply@medassure.com
SES_REGION=us-east-1

# Agents Service Configuration
# Backend connects to agents service which handles all Jira/Bedrock/MCP operations
AGENTS_SERVICE_URL=http://localhost:8001
